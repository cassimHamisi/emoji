import { Text, View } from "react-native";

export function EmojiOne({ navigation }: { navigation: any }) {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Emoji One Screen!</Text>
    </View>
  );
}

import { Text, View } from "react-native";

export function EmojiTwo({ navigation }: { navigation: any }) {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Emoji Two Screen!</Text>
    </View>
  );
}
